schema {
    mutation: Mutation
    query: Query
}

type Query {
    users: [User]!
    user(id: String!): User
    documents: [Document]!
    document(id: String!): Document
}

type Mutation {
    test: String
}
